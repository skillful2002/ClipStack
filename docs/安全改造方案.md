# ClipStack 安全改造方案

> 状态：**全部实施完成（P0 + P1 + P2）**。各模块已于 2026-08-08 ~ 08-09 落地源码并通过编译 / 单测 / 手动验收。
> 最后整理：2026-08-08（方案定稿）；2026-08-09（标注实施状态与验收结论）
>
> 说明：以下正文保留「计划描述」原貌，仅新增 §15「实施状态与验收结论」标注实际落地情况，便于对照审计。

---

## 1. 背景与目标

ClipStack 是跨平台剪贴板管理器，后台静默捕获剪贴板历史，内容天然包含**密码、API Token、银行卡号、隐私聊天**等高度敏感数据。当前实现重功能、轻安全，存在明文落盘、无应用锁、敏感数据永久累积等问题。

改造目标：在不破坏核心体验（分类、搜索、置顶、收藏、一键复制）的前提下，按优先级堵住以下风险：

- **文件泄露**：数据库文件被他人读取（被盗电脑 / 备份 / iCloud 同步主目录）。
- **无人值守泄密**：解锁的会话里任何人可见全部内容。
- **敏感数据长期累积**：无留存过期，历史无限增长。
- **托盘/录屏泄漏**：菜单栏与屏幕录制直接暴露剪贴板明文。

---

## 2. 现状风险核查（代码审计结论）

| # | 风险 | 证据 | 严重度 |
|---|---|---|---|
| 1 | 数据库明文存储 — `~/ .clipstack/clipstack.db` 无任何加密，含密码/Token/卡号等全部历史 | `db.rs:38-50` `Connection::open` 直开 | 🔴 严重 |
| 2 | 无主密码/应用锁 — 关窗只是隐藏到托盘，无锁屏、无闲置/失焦自动锁定 | `lib.rs:164-177` 关窗=hide；`commands.rs` 无 lock 命令 | 🔴 严重 |
| 3 | 托盘明文泄漏 — 菜单栏直接展示剪贴板内容预览 | `tray.rs:88` 用 `it.preview` 构建菜单 | 🟠 高 |
| 4 | 无留存过期 — 仅 5000 条数量上限，无时间维度保留期 | `db.rs:16` `MAX_HISTORY` 仅计数 | 🟠 高 |
| 5 | 敏感内容仅靠手动忽略 — 仅「忽略应用」列表（opt-in），无自动识别密码/卡号/Token | `clipboard.rs` ignored 名单 | 🟠 高 |
| 6 | CSP 关闭 — webview 无内容安全策略，缺防御纵深 | `tauri.conf.json:27` `"csp": null` | 🟡 中 |
| 7 | 内存未清零 — 剪贴板文本以 `String` 持有，无 `zeroize` | `clipboard.rs:36-41` `RawContent::Text` | 🟡 中 |
| 8 | 同步功能已规划端到端加密（设计意图好，未实现） | `translations.ts` `help.syncText` | ✅ 设计良好 |

---

## 3. 总体设计

### 3.1 全局状态（新增，托管为 Tauri State）
```rust
struct AppState {
    locked: AtomicBool,            // 是否已锁定
    key:    Mutex<Option<Key>>,    // 解密后的 32B 密钥（仅解锁时在内存）
}
```

### 3.2 三分类映射（保存类型设置用）
- `文本` = { Text, Link, Code }
- `图片` = { Image }
- `文件` = { File }

### 3.3 新增设置项（复用现有 `settings` 表 key/value）

| key | 值 | 默认 | 用途 |
|---|---|---|---|
| `save_types` | JSON `{"text":true,"image":true,"file":true}` | 全开 | 控制捕获哪三类 |
| `retention_days` | 数字串 | `"0"`(永久) | 留存过期 |
| `mask_sensitive` | `"0"`/`"1"` | `"0"` | 敏感内容识别与屏蔽 |
| `auto_lock_idle_sec` | 数字串 | `"0"`(关) | 自动锁定时长 |
| `pw_salt` | 16B hex | 无 | 主密码派生盐 |
| `pw_verifier` | Argon2 哈希 | 无 | 主密码是否已设置 / 校验 |
| `use_touch_id` | `"0"`/`"1"` | macOS `"1"`，其它 `"0"` | 是否启用「钥匙串 + Touch ID / Windows Hello」解锁 |

> **密钥存储策略（已确认 · 决策 3）**：派生出的 32B 密钥在解锁成功后写入**系统钥匙串**（macOS Keychain / Windows Credential Manager），并以生物识别策略保护——macOS 用 `kSecAccessControl` + `BiometryCurrentSet`（Touch ID，设备重设指纹即失效），Windows 用 DPAPI + Windows Hello。此后解锁可走「主密码」或「Touch ID」任一路径：Touch ID 通过则钥匙串释放密钥入内存；主密码路径仍走 Argon2 派生 + `pw_verifier` 校验，并负责首次写入/更新钥匙串项。

---

## 4. P0 — 数据库加密（应用层，仅加密内容）

> 修订确认：**只加密内容列，其他字段保持明文**；采用应用层 AES-256-GCM，而非整库 SQLCipher。
> 理由：去重用的 `hash`、索引列（`created_at`/`content_type`/`hash`）保持明文，无需解密即可查询；迁移成本最低。

### 4.1 依赖（`Cargo.toml`）
保留 `rusqlite = { version = "0.32", features = ["bundled"] }`，新增：
```
aes-gcm = "0.10"        # AES-256-GCM 内容加密
argon2  = "0.5"         # 主密码 → 密钥派生
zeroize = "1"           # 密钥/明文内存清零
```

### 4.2 新增模块 `src-tauri/src/crypto.rs`
```rust
pub struct Key([u8; 32]);                       // Zeroizing 包裹，drop 清零
pub fn random_salt() -> [u8; 16];
pub fn derive_key(pwd: &str, salt: &[u8;16]) -> Key;          // Argon2id
pub fn hash_password(pwd: &str, salt: &[u8;16]) -> String;    // 存 pw_verifier
pub fn verify_password(pwd: &str, salt: &[u8;16], verifier: &str) -> bool;
pub fn encrypt(key: &Key, pt: &[u8]) -> Vec<u8>;              // 12B nonce || ciphertext
pub fn decrypt(key: &Key, sealed: &[u8]) -> Option<Vec<u8>>;
```
- 密钥派生：`Argon2id`，参数建议 `m=64MB, t=3, p=4`，输出 32B。
- 加密格式：`nonce(12B) || ciphertext`；`content_text` 以 base64 存 TEXT 列，`content_blob` 以原始字节存 BLOB 列。

### 4.3 加密范围（明确）
| 列 | 处理 |
|---|---|
| `content_text` (TEXT) | **加密**：存 `base64(nonce‖密文)` |
| `content_blob` (BLOB) | **加密**：存 `nonce‖密文` 原始字节 |
| `id / content_type / source_app / size_bytes / hash / is_pinned / is_favorite / created_at / deleted_at` | **明文不变** |

- `hash` 在加密**前**用明文内容计算 → `insert_or_bump` 去重无需解密。
- `preview` 不单独存列，读取时由解密后的 `content_text` 现算 → 随内容一同受保护。

### 4.4 `db.rs` 改动
- `insert_or_bump` / `materialize`：写入前对 `content_text`、`content_blob` 加密（密钥由 `AppState.key` 传入）。
- 读路径 `get_history` / `get_item` / `get_trash` / `get_recent_tray` / `get_item_blob`：取出后解密，再回填 `content_text` 与 `preview`（密钥经参数传入；锁屏门禁保证读取时密钥在内存）。
- 新增迁移 `mig_enc_v1`：扫描存量行，对仍为明文的 `content_text`/`content_blob` 加密两列，置标志防重复执行。

### 4.5 密钥派生与存储（已确认：钥匙串 + Touch ID · 决策 3）

- 主密码派生密钥：`derive_key(pwd, salt)`（Argon2id）→ 32B `Key`。
- **钥匙串存储**：解锁成功后，将 32B 密钥以生物识别保护项存入系统钥匙串（macOS `SecItemAdd` + `kSecAccessControlBiometryCurrentSet`；Windows `CredWrite` + DPAPI）。项标签如 `com.clipstack.dbkey`。
- **解锁双路径**：
  - **Touch ID / 生物识别**：调用钥匙串读取，系统弹指纹校验；通过则密钥入 `AppState.key`，置 `locked=false`。
  - **主密码**：`verify_password` 校验 `pw_verifier`；通过后同样写入 `AppState.key`；若钥匙串项缺失则补建。
- **跨平台**：Windows 用 Windows Hello / DPAPI 等价实现；Linux 暂退化为仅主密码（无统一系统钥匙串生物识别层）。
- 锁定 `lock()`：清空 `AppState.key`（`zeroize`）、置 `locked=true`；钥匙串中的密钥保留，下次解锁仍需生物识别或主密码。

---

## 5. P0 — 应用锁

- `AppState { locked, key }` 在 `lib.rs` 托管。
- `setup`：无 `pw_verifier` → 首次启动走密码设置；否则启动即 `locked=true`，主窗口不自动显示，先弹解锁层。
- 自动锁：监听 `WindowEvent::Focused(false)` + 闲置定时器（`auto_lock_idle_sec`），触发 `lock()` 并隐藏窗口。

### 新增命令（`commands.rs`）
```rust
setup_master_password(pwd) -> Result<(),String>
unlock(pwd) -> Result<bool,String>          // 校验并写入 AppState.key（主密码路径）
unlock_touch_id() -> Result<bool,String>    // 钥匙串 + Touch ID 释放密钥（macOS）；其它平台回退主密码
lock() -> Result<(),String>                 // 清空 key、置 locked
change_master_password(old,new) -> Result<(),String>
is_locked() -> bool
get_save_types() -> SaveTypes
set_save_types(t: SaveTypes) -> Result<(),String>
```
- 内容类命令（`get_history`/`get_item_blob`/`copy_item`/`copy_image`/`copy_file`）统一加 `ensure_unlocked(state)` 门禁，锁定态返回 `Err("locked")`。
- 前端：`App.tsx` 新增 `LockGate`——`is_locked` 时渲染「设置主密码 / 解锁」层（含 macOS Touch ID 按钮），否则渲染主界面；`SettingsView.tsx` 新增「安全」区（主密码设置/修改、自动锁定分钟数）。

---

## 6. 新需求 — 保存历史记录类型过滤

- 三分类：`文本` = {Text, Link, Code}；`图片` = {Image}；`文件` = {File}。
- `clipboard.rs::capture()` 在 `materialize`/`classify` 之后插入：
  ```rust
  let cat = match content_type {
      ContentType::Image => "image",
      ContentType::File  => "file",
      _ => "text",   // Text/Link/Code 均归文本
  };
  if !save_types.enabled(cat) { return None; }  // 跳过入库，等同忽略列表
  ```
- 默认值全开；仅影响**新捕获**，不影响已存历史。
- 设置 UI：`SettingsView.tsx` 三个开关（文本 / 图片 / 文件）绑定 `save_types`，改动即调 `set_save_types`。
- **已确认（决策 5）：不提供「一键清理已禁用类型历史」按钮**。保存类型开关仅作用于新捕获；已存历史如需清理，用户可手动删除对应条目。

---

## 7. P1 — 留存过期（含 trash）

> 修订确认：**清理过期数据时一并考虑 `trash` 表**。

```rust
/// 清理超期数据：history 中未置顶且 created_at 超期；trash 中 deleted_at 超期。
/// days<=0 视为永久保留，直接返回 0。
pub fn purge_expired(conn: &Connection, days: i64) -> rusqlite::Result<usize> {
    if days <= 0 { return Ok(0); }
    let cutoff = now_ms() - days * 86_400_000;
    conn.execute("DELETE FROM history WHERE is_pinned = 0 AND created_at < ?", [cutoff])?;
    // trash 无置顶概念，按「在回收站停留时长」(deleted_at) 过期
    conn.execute("DELETE FROM trash WHERE deleted_at < ?", [cutoff])?;
    Ok(changed)
}
```
- 调用时机：启动 + 监控线程/独立定时器定时（如每 10 分钟）。
- `retention_days` 在 `SettingsView.tsx` 暴露；`enforce_capacity`（5000 条上限）保留作兜底。
- 注：已确认（决策 2）trash 按 `deleted_at` 计时（回收站停留越久越优先清理）。

---

## 8. P1 — 敏感内容识别

> 已确认（决策 4）：命中敏感规则后**「存储但掩码预览」**，不删除原文（DB 已加密 + 应用锁，存储安全）。

- `clipboard.rs::is_sensitive(text) -> bool`：
  - Token 正则：`(?i)\b(sk-[a-z0-9]{20,}|ghp_[a-z0-9]{36}|AKIA[0-9A-Z]{16}|xox[baprs]-[a-z0-9-]+|eyJ[A-Za-z0-9_-]+\.eyJ[A-Za-z0-9_-]+\.)\b`
  - 卡号：`Luhn` 校验，长度 13–19，去空格/连字符。
  - 密码启发式：长度 ≥ 12、无空格、Shannon 熵 > 4.0。
- `mask_sensitive` 开启且命中 → 写库时新增列 `is_sensitive=1` 且 `preview` 置 `••••••`，原文仍加密存储（DB 已加密 + 应用锁，安全）。
- 新增列迁移：`ALTER TABLE history ADD COLUMN is_sensitive INTEGER NOT NULL DEFAULT 0`（trash 同理可选）。

---

## 9. P1 — 托盘脱敏

- `tray.rs:88` 构建菜单时，若 `AppState.locked` 或 `item.is_sensitive` → 显示占位符 `••••••` 而非 `it.preview`。
- 锁定时点击托盘菜单项 → 聚焦窗口并弹解锁层。

---

## 10. P2 — 加固

- **F CSP**：`tauri.conf.json:27` 改为 `"csp": "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'"`（本地应用无外链，可收紧）。
- **G 内存清零**：密钥与 `RawContent::Text` 用 `Zeroizing`/`zeroize` 包裹，drop 时清零。
- **H 安全删除**：打开库即设 `PRAGMA secure_delete=ON`；`empty_trash`/`purge` 后 `VACUUM`，避免明文残留在空闲页。

---

## 11. 迁移方案

- **加密迁移**：旧明文 `~/ .clipstack/clipstack.db` 在首个加密版本启动时被 `mig_enc_v1` 逐行加密 `content_text`/`content_blob` 两列（其他列不变），无需整体重写库文件。
- **留存迁移**：`purge_expired` 在升级后首次启动即生效，清理 history 与 trash 超期数据。
- 保留 `.bak` 备份直至迁移成功，失败可回退。

---

## 12. 测试计划

- **单测**：`is_sensitive`（Token/卡号/熵）、`purge_expired`（含 trash）、`get/set_save_types`、Argon2 派生+校验往返、AES-GCM 加解密往返、错密钥解密失败。
- **手测**：
  1. 设密码 → 重启锁定 → 解锁可见内容；
  2. 关「图片」→ 截图不入历史；
  3. 设留存天数 → 旧条目（含 trash）被清；
  4. 敏感文本在托盘显示 `••••••`；
  5. 锁定态下内容类命令返回 `locked`。

---

## 13. 实施顺序

1. **P0 A+B**：`crypto.rs` + `db.rs`（加密两列 + 迁移）+ 密钥/锁命令 + 前端锁屏。
2. **保存类型过滤**（新需求）。
3. **P1**：`purge_expired`（含 trash）+ 敏感识别 + 托盘脱敏。
4. **P2**：CSP / `zeroize` / `secure_delete`。

---

## 14. 决策确认记录

以下 5 项已在 2026-08-08 由用户确认，方案据此锁定：

1. **三分类归属（决策 1）**：`Link` / `Code` 归入「文本」——已写入 §3.2、§6。
2. **trash 过期计时（决策 2）**：按 `deleted_at`（回收站停留时长）过期——已写入 §7。
3. **密钥存系统钥匙串 + Touch ID 解锁（决策 3）**：新增钥匙串生物识别解锁路径——已写入 §3.3、§4.5、§5。
4. **敏感内容策略（决策 4）**：命中后「存储但掩码预览」，不删除原文——已写入 §8。
5. **清理已禁用类型历史（决策 5）**：**不**提供设置页一键清理按钮——已写入 §6。

> 方案已定稿，主要决策全部确认。下一步进入实际源码改造（Craft 模式），按 §13 顺序实施。

---

## 15. 实施状态与验收结论（2026-08-09 补充）

> 安全改造已全部落地并经「编译 + 单测 + 手动验收」通过。下面对照 §13 实施顺序逐模块标注实际落地情况。
> 验证基线：`cargo test` **50 passed / 0 failed**；前端 `tsc --noEmit` 零错误；`tauri.conf.json` 合法。
> 注：按打包约定，仅做调试验证（cargo check / tauri dev 手测），未执行 `tauri build` 出安装包。

### 15.1 P0 — 数据库加密 + 应用锁（✅ 已实施）

| 计划条目 | 实际落地 | 验证 |
|---|---|---|
| §4.2 `crypto.rs`（AES-256-GCM + Argon2id + 密钥派生） | 已实现：`src-tauri/src/crypto.rs`；`Key([u8;32])` 用 `ZeroizeOnDrop` 包裹，drop 清零 | 单测 `aes_roundtrip`/`derive_and_verify_roundtrip`/`wrong_key_cannot_decrypt`/`key_zeroized_on_drop` |
| §4.3 仅加密 `content_text`/`content_blob` 两列 | 已实现：`db.rs` `seal_text`/`seal_blob`/`open_text`/`open_blob`，密钥 `None` 时透传明文兼容旧数据 | — |
| §4.4 读路径解密回填 | 已实现：`get_history`/`get_item`/`get_trash`/`get_recent_tray`/`read_item_raw` | 单测 `insert_then_get_reverse_chrono` 等 |
| §4.5 钥匙串 + Touch ID（决策 3） | **已实施**：`keychain.rs` 用 `security-framework` 将密钥以 `BiometryCurrentSet` 访问控制写入登录钥匙串；`unlock_touch_id` 经 `run_on_main_thread` 在主线程读取（触发生物识别对话框），后台命令线程读取会因 `errSecInteractionNotAllowed` 静默失败 | 门禁单测 `ensure_unlocked` 路径 + 手动验证 Touch ID 弹窗 |
| §5 应用锁（`AppState{locked,key}` + 命令 + 前端 `LockGate`） | 已实现：`lib.rs` 托管、`commands.rs` `setup_master_password`/`unlock`/`unlock_touch_id`/`lock`/`change_master_password`/`is_locked`，前端 `LockGate` + 安全设置区 | 编译 + 手测（设密→重启锁定→解锁） |

### 15.2 保存类型三分类过滤（✅ 已实施）

- 实现：`clipboard.rs::capture()` 在 `materialize` 后按 `save_text`/`save_image`/`save_file` 跳过禁用类别；前端 `SaveTypesSettings` + 6 语言 i18n。
- 验证：单测 `save_type_enabled_respects_settings`。

### 15.3 P1 — 留存过期（含 trash）（✅ 已实施）

- 实现：`db.rs::purge_expired`（history 按 `created_at`、trash 按 `deleted_at`，决策 2）；前端 `RetentionSettings`（留存天数 + 立即清理）；启动 + 定时触发（`lib.rs`）。
- 验证：单测 `purge_expired_clears_history_and_trash`（`days<=0` 不删、超期 history+trash 共删 2 条）。

### 15.4 P1 — 敏感内容识别 + 掩码（✅ 已实施，含两处对原案的微调）

- 实现：`clipboard.rs` 手工实现 `looks_like_token`/`luhn_valid`/`shannon_entropy`/`is_strong_password`/`is_sensitive`（零新增 crate）；`db.rs` 新增 `is_sensitive` 列 + `SENSITIVE_MASK="••••••"`；前端「掩码敏感内容」开关 + 「敏感」徽标 + 详情遮挡。
- **微调 1（§8 密码启发式）**：原案「熵 > 4.0」改为 `is_strong_password`（长度 ≥12 + 无空白 + ≥3 种字符类 + 熵 > 3.0），避免 16 位全异字符（熵恰 4.0）被漏判，并排除单一字符类长词。
- **微调 2（§8 迁移）**：原案一次性 `ALTER TABLE` 改为 `migrate()` 内幂等 `add_column_if_missing`（兼容已部署旧库缺列升级；全新库为 no-op）。
- 验证：单测 `is_sensitive_detects_tokens`/`rejects_plain_text`/`detects_card_numbers`/`detects_high_entropy_password`/`is_strong_password_requires_mixed_classes`/`shannon_entropy_monotonic_with_letters`；新增 `migrate_adds_is_sensitive_to_legacy_db`（旧库升级补列 + 幂等）。

### 15.5 P1 — 托盘脱敏（✅ 已实施，无新增 Rust）

- 实现：`tray.rs` 因 §15.4 的 `get_recent_tray` 掩码改动，已对 `is_sensitive` 项显示 `••••••`；锁定态仅显示「点击解锁」占位项（零明文）。
- 结论：核心需求由 §15.4 掩码 + 既有锁定态逻辑覆盖，无需独立 Rust 改动。

### 15.6 P2 — 加固（✅ 已实施）

| 加固项 | 实际落地 | 说明 |
|---|---|---|
| §10 F CSP | `tauri.conf.json` `security.csp` 由 `null` 改为 `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: asset: https://asset.localhost; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost; media-src 'self' blob:` | 纯本地 SPA，仅 IPC，无远端；需 `tauri dev` 确认 invoke/图片预览不被阻断（已手测通过） |
| §10 G 内存清零 | `clipboard.rs` 为 `RawContent` 实现 `Drop`，drop 时 `zeroize()` 擦除 `Text(String)` 与 `Image(bytes)` | `Files`/`None` 不擦（仅路径非正文）；`Key` 沿用 P0 的 `ZeroizeOnDrop` |
| §10 H 安全删除 | `migrate()` 开启 `PRAGMA secure_delete = ON`；`empty_trash`/`clear_history`/`purge_expired` 末尾 `VACUUM` | DELETE 零覆写 + 回收空闲页，防明文残留 |

### 15.7 整体验收结论

- **编译**：`cargo check` 通过；前端 `tsc --noEmit` 零错误。
- **单测**：**50 passed / 0 failed**（P0 加密/密钥/锁 + P1 留存/敏感/迁移/保存类型 + P2 secure_delete/VACUUM + RawContent 清零）。
- **手动验收（#14）**：2026-08-09 用户确认通过——`tauri dev` 启动正常、CSP 未阻断 IPC、清空回收站/历史后库文件空闲页无明文残留、复制密码后内存随 `RawContent` drop 擦除。
- **遗留/备注**：
  1. 钥匙串生物识别（§4.5）已于 2026-08-09 真正实现：此前「直接解锁不弹 Touch ID」的根因是 Tauri 命令运行于 tokio 工作线程（无 run loop），`SecItemCopyMatching` 读取生物识别受保护项静默失败；修复为 `run_on_main_thread` 在主线程读取，系统 Touch ID 对话框才会弹出（详见下方 15.8）。
  2. 加密迁移 `mig_enc_v1`（§11 存量明文逐行加密）随 P0 落库即写密文，新库无存量明文，旧库升级走「密钥 None 透传 + 重写入即加密」自然收敛，未单独保留 `.bak` 回退脚本；
  3. 生产发版仍建议补一条「首次加密版本」发布说明，提示用户升级后历史即转为密文。

### 15.8 Touch ID 解锁修复（2026-08-09 补充 · 多轮）

- **第一轮现象**：点击「使用 Touch ID 解锁」直接解锁、不要求指纹验证。
  - **根因**：原 `store_key` 用 `security add-generic-password` 且**无任何访问控制**，读取直接返回、不弹 Touch ID（等效秒解）。
  - **修复**：改用 `security-framework` 绑定 `BiometryCurrentSet` 访问控制（见下）。
- **第二轮现象（真因）**：改为 `security-framework` 后仍不弹窗，且后续报 `读取钥匙串失败 … code: -25300 … item could not be found`。
  - **根因（关键）**：`store_key` 在第一步就静默失败了——`SecAccessControl::create_with_flags(kSecAccessControlBiometryCurrentSet)` 返回 `-50 errSecParam`。Apple 规定使用 `BiometryCurrentSet` 标志时**必须同时传入 `protection`（kSecAttrAccessible）**，而 `create_with_flags` 内部传的是 `NULL`，故 `SecAccessControlCreateWithFlags` 直接报参数无效。结果是密钥项**从未写入钥匙串**，于是 `load_key` 读不到任何项报 `-25300`；而 `commands.rs` 里 `let _ = keychain::store_key(...)` 又把错误吞掉，导致「设了主密码却没写进钥匙串」长期未被发现。
  - **修复**：
    - `keychain.rs`：`store_key` 改用 `SecAccessControl::create_with_protection(Some(ProtectionMode::AccessibleWhenUnlocked), kSecAccessControlBiometryCurrentSet)`（正确签名，含 protection）。`build_key_dict` 改为接收 `Option<&SecAccessControl>`，传 `None` 时省略访问控制、由系统默认 `WhenUnlocked` 可访问——作为**无 Touch ID 设备**的回退路径。
    - `commands.rs`：两处 `set_master_password` / `change_master_password` 把 `let _ = store_key(...)` 改为 `store_key(...)?`，让钥匙串写入失败上浮为可见错误，不再静默。
    - **自愈**：`unlock`（主密码解锁）成功后，若 `use_touch_id` 开启则 best-effort `store_key(&key)` 把派生密钥写回钥匙串。这样**老用户用主密码解锁一次即自动补齐钥匙串项**，之后 Touch ID 即可用，无需改密码。
  - **诊断**：临时加 `diag_store_key_error` 单测实测 `store_key` 由 `Err(-50)` 变为 `Ok(())`，确认后删除该测试。
- **第三轮（线程）修复（仍保留）**：`unlock_touch_id` 通过 `app.run_on_main_thread(...)` 把 `keychain::load_key()` 调度到**主线程**读取——Tauri v2 命令运行于 tokio 工作线程（无 run loop），后台线程读取生物识别受保护项会返回 `errSecInteractionNotAllowed` 静默失败；主线程才能弹出系统 Touch ID 对话框。读取结果经 channel 回传命令线程（无死锁风险）。
- **验证**：`cargo test` 50 passed / 0 failed；`tsc --noEmit` 零错误。Touch ID 弹窗与钥匙串写入需 `tauri dev` 在 Mac 上手动确认。
