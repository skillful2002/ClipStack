# Changelog

本文档记录各发布版本的变更内容，按版本倒序排列。

## v0.2.3 - 2026-08-16

侧边栏交互重构与原生菜单多语言版本。

### ✨ 新功能

- **侧边栏左下角入口图标化**：主界面 / 设置 / 回收站 / 帮助 / 关于 改为一排 5 个图标按钮（等分宽度、内容居中），悬停显示功能名称，设置与回收站提示含快捷键（⌘, / ⌘⇧T），新版本角标显示在「关于」按钮。
- **顶部菜单栏原生菜单多语言**：macOS 顶部菜单栏改为自定义原生菜单，菜单项文案随应用语言切换（不再固定英文）。

### 🎨 界面与交互优化

- 主界面按钮图标由窗口字形改为 Home 房子图形。
- 左侧来源排行由 10 个增加到 12 个。

### 🔧 构建 / 杂项

- 升级版本号至 0.2.3。

## v0.2.2 - 2026-08-15

局域网共享在线设备列表去重增强与多项交互优化版本。

### ✨ 新功能

- **局域网共享在线列表去重增强**：mDNS TXT 广播新增本机 IP 字段，在线设备列表按「机器名 + IP」双重判定去重；同一台机器的多连接收敛为一条展示，对局域网内同名不同 IP 的真机（克隆镜像 / 批量部署）零误伤。
- 列表 / 详情按钮悬浮功能提示改用自定义 Tooltip 组件（替代原生 `title`，修复 macOS WKWebView 下提示不显示或延迟的问题）。
- 敏感内容识别重写并改为读取时实时重算，掩码开关切换对新旧所有条目即时生效。
- 详情面板交互优化与代码语法高亮。
- 链接项悬浮菜单新增「用默认浏览器打开」。
- 时间过滤新增「本月」与「指定日期」。
- 中间列表变化时始终选中首行。
- 左侧来源过滤与搜索清除按钮。

### 🐛 修复

- 修复 Windows 下共享密钥不持久化（每次共享需重输密码）。
- 局域网共享：新增无名连接守卫，过滤幽灵设备，避免在线列表显示设备 UUID。
- 局域网共享：在线设备列表去重改用「IP + 机器名」双重判定。
- 未设置主密码时「设置主密码」按钮改为绿边绿字描边样式。
- 修复切换查询条件后中间列表滚动回顶部。
- 修复文件共享冲突命名序号置于后缀之前。

### 🎨 界面与交互优化

- 设置面板「局域网共享」重组：在线设备列表移至共享开关之后、共享组之前并始终显示。
- 侧边栏分类数量徽标固定宽度、快捷键对齐。
- 设置界面按钮与主题选中样式优化。

### 🔧 构建 / 杂项

- 修正 macOS dmg 构建脚本：修正 workspace 产物路径，版本号改为从 `tauri.conf.json` 动态读取。
- 升级版本号至 0.2.2。

### 📚 文档

- 新增局域网共享五大运行时机制梳理文档（发现 / 连接 / 断开 / 首次打开 / 在线列表）。
- README 补充后续优化与产品规划清单。

## v0.2.1 - 2026-08-13

局域网共享（LAN 同步）稳定性修复版本。

### 🐛 修复

- 修复开启共享后托盘菜单卡死主线程（`block_on` 驱动 async 改为 `spawn`）。
- 修复 `start()` 持有 inner 锁期间 await 连接重试导致的死锁（将手动对端重连移出锁外）。
- 修复关闭共享后在线设备仍显示、下线又复活（对端连接发送 Close 帧秒级下线、中止重宣告任务）。
- 修复克隆机器 `device_id` 碰撞导致互相看不到对端（检测到碰撞自动重新生成并持久化、重启共享）。
- 修复关闭一端再打开后另一端显示两台相同设备（新增自连守卫，拒绝连接本机 `device_id`）。
- 修复「清除记录」报 `UNIQUE constraint failed: trash.id`（trash 表 id 改为自增，与 history 解耦）。
- 提升 mDNS 发现速度（有限次周期性重宣告 2s / 7s / 15s）。

## v0.2.0 - 2026-08-11

局域网共享（LAN 同步）正式落地版本。

### ✨ 新功能

- **局域网共享（LAN 同步）**：同一局域网（WiFi / 有线）内多设备端到端加密互享剪贴板，无服务器、不上云（设计见 `docs/clipstack-lan-sync-design.md`）。
- **设置面板「局域网共享」**：共享组、共享密钥、本机共享开关、文件大小上限、共享类型过滤、手动对端、组内在线设备列表、来源标识（设备名）。
- **托盘一键开关**：托盘菜单新增「共享」项，状态圆点（亮=开 / 暗=关）+「已开启·点击关闭 / 已关闭·点击开启」文字，点击切换并实时同步前端设置页。
- **开启前置校验**：开启共享前必须已设置共享组、共享密钥、端口三者，缺失则拦截并提示。
- **大文件 / 目录包传输**：32KB 分片传输（START/DATA 帧），支持大文件与 macOS 目录包（如 `.rtfd`）完整互传。
- **历史「另存为」**：图片存 PNG、单文件 / 多文件 / 文件夹保存到本地指定位置（基于 `tauri-plugin-dialog`）。

### 🐛 修复

- 修复共享类型（文本 / 图片 / 文件）过滤不生效。
- 修复接收端大小上限不生效、来源显示设备 ID（改为设备名）。
- 修复托盘切换共享开关无效、状态圆点不变色（改用原子标志位同步读取，避免 `block_on` 嵌套）。
- 修复「局域网共享」中修改共享组 / 密钥 / 端口 / 手动对端后切换设置页未保存，必须再次切换「共享本机剪贴板」才会落库的问题（改为失焦即保存）。

## v0.1.2 - 2026-08-09

从 v0.1.1 以来的安全能力升级（基于提交 `909814b9`、`234f25a4` 落地），聚焦本地加密、应用锁与隐私加固。

### ✨ 新功能

- **本地数据库加密**：剪贴板正文与图片 / 文件二进制以 AES-256-GCM 在本地 SQLite 加密存储（仅 `content_text` / `content_blob` 两列加密，密钥存 `~/.clipstack/dbkey.dat`，权限 600）；旧库明文在解锁后自动转密文，无需手动迁移。
- **应用锁 + Touch ID 解锁**：可设置主密码应用锁（Argon2id 校验），支持手动锁定、窗口失焦锁定、闲置自动锁定；macOS 支持用系统 Touch ID / 登录密码解锁（经 `LocalAuthentication` / LAContext，钥匙串不存解锁令牌）。
- **敏感内容识别与掩码**：零新增依赖手工实现（Token 形态 / Luhn / 强密码启发式），命中密码 / Token / 卡号的内容在列表与详情中以 `••••••` 遮挡预览，原文仍加密且可复制（掩码按读取时实时计算，开关即时生效）。
- **留存过期**：新增保留天数设置，超期历史（未置顶）与回收站内容自动清理。
- **保存类型三分类**：新增文本 / 图片 / 文件 捕获开关，按类型过滤记录。
- **托盘脱敏**：锁定态与敏感项在托盘菜单显示占位，零明文泄漏；托盘菜单「使用帮助」图标与设置同色系。

### 🐛 修复

- 修复锁定流程：托盘解锁直接弹出锁屏界面，立即锁定可关闭主窗口；锁定时内容类命令返回锁定错误。

### 🔒 安全加固

- **密钥职责分离**：数据库加密密钥与用户主密码完全解耦——主密码仅作应用锁凭据，不派生、不持有、不清除数据库密钥；锁定态仍照常加密捕获。
- **P2 加固**：收紧 CSP；清空 / 删除后 `secure_delete=ON` + `VACUUM` 防明文残留；`RawContent` 实现 `Drop` 用 `zeroize` 擦除明文缓冲。

### 📚 文档

- 新增「安全与隐私」帮助章节（应用内帮助与 `docs/USER_GUIDE.md`）；设计文档 `docs/安全改造方案.md` 按实际实现（内部文件密钥 + 主密码仅应用锁 + Touch ID 走 LAContext）修订；README / 开发文档补充安全特性与 `cargo test` 50/50 质量门禁。

> 说明：跨设备同步（「同步组」端到端加密共享）仍为下个大版本规划，本版本尚未上线。

## v0.1.1 - 2026-08-08

从 v0.1.0 以来的累计更新，聚焦于易用性、多语言与文档完善。

### ✨ 新功能

- **多语言使用帮助**：主页侧边栏与托盘菜单新增「帮助」入口，内置使用说明（安装、基本操作、分类与搜索、快捷键、设置、回收站与托盘菜单等）；内容接入 i18n，支持简体中文、繁体中文、English、日本語、Deutsch、Français，切换语言时自动更新。
- **清除按查询条件**：「清除全部」改为按当前筛选条件清除（分类 / 时间 / 搜索），避免误清整库。
- **快捷键可视化**：菜单、按钮、托盘菜单显示快捷键提示；新增 `src/lib/shortcuts.ts` 统一管理；搜索快捷键由 `⌘K` 调整为 `⌘/`（原组合易被其它程序占用）。
- **全新图标**：应用图标与系统托盘图标更换为新设计。

### 🐛 修复

- 托盘菜单链接图标与主界面历史列表中的 `TypeIcon link` 保持一致（互锁双环样式）。
- 托盘菜单 `help / about / settings / open` 图标统一为中性灰，修复亮色模式下「关于系统」图标不可见的问题。

### 📚 文档

- 新增《跨设备同步技术设计文档》（`docs/clipstack-sync-design.md`），含架构拓扑、同步数据流、配置模型、组共享模型四张 SVG 图示。
- README 新增公众号关注章节；修正「简单体中文」笔误为「简体中文」；补充语言说明。

### 🔧 构建 / 杂项

- 升级 macOS 打包脚本，支持多架构（universal / x64 / aarch64）并修正产物路径。

> 说明：跨设备同步（「同步组」端到端加密共享）为下个大版本规划，本版本尚未上线，相关描述已从帮助与用户指南中移除。
