# Changelog

本文档记录各发布版本的变更内容，按版本倒序排列。

## v0.1.2 - 2026-08-09

从 v0.1.1 以来的安全能力升级（基于提交 `909814b9`、`234f25a4` 落地），聚焦本地加密、应用锁与隐私加固。

### ✨ 新功能

- **本地数据库加密**：剪贴板正文与图片 / 文件二进制以 AES-256-GCM 在本地 SQLite 加密存储（仅 `content_text` / `content_blob` 两列加密，密钥存 `~/.clipstack/dbkey.dat`，权限 600）；旧库明文在解锁后自动转密文，无需手动迁移。
- **应用锁 + Touch ID 解锁**：可设置主密码应用锁（Argon2id 校验），支持手动锁定、窗口失焦锁定、闲置自动锁定；macOS 支持用系统 Touch ID / 登录密码解锁（经 `LocalAuthentication` / LAContext，钥匙串不存解锁令牌）。
- **敏感内容识别与掩码**：零新增依赖手工实现（Token 形态 / Luhn / 强密码启发式），命中密码 / Token / 卡号的内容在列表与详情中以 `••••••` 遮挡预览，原文仍加密且可复制（掩码按读取时实时计算，开关即时生效）。
- **留存过期**：新增保留天数设置，超期历史（未置顶）与回收站内容自动清理。
- **保存类型三分类**：新增文本 / 图片 / 文件 捕获开关，按类型过滤记录。
- **托盘脱敏**：锁定态与敏感项在托盘菜单显示占位，零明文泄漏；托盘菜单「使用帮助」图标与设置同色系。

### 🐛 修复

- 修复锁定流程：托盘解锁直接弹出锁屏界面，立即锁定可关闭主窗口；锁定时内容类命令返回锁定错误。

### 🔒 安全加固

- **密钥职责分离**：数据库加密密钥与用户主密码完全解耦——主密码仅作应用锁凭据，不派生、不持有、不清除数据库密钥；锁定态仍照常加密捕获。
- **P2 加固**：收紧 CSP；清空 / 删除后 `secure_delete=ON` + `VACUUM` 防明文残留；`RawContent` 实现 `Drop` 用 `zeroize` 擦除明文缓冲。

### 📚 文档

- 新增「安全与隐私」帮助章节（应用内帮助与 `docs/USER_GUIDE.md`）；设计文档 `docs/安全改造方案.md` 按实际实现（内部文件密钥 + 主密码仅应用锁 + Touch ID 走 LAContext）修订；README / 开发文档补充安全特性与 `cargo test` 50/50 质量门禁。

> 说明：跨设备同步（「同步组」端到端加密共享）仍为下个大版本规划，本版本尚未上线。

## v0.1.1 - 2026-08-08

从 v0.1.0 以来的累计更新，聚焦于易用性、多语言与文档完善。

### ✨ 新功能

- **多语言使用帮助**：主页侧边栏与托盘菜单新增「帮助」入口，内置使用说明（安装、基本操作、分类与搜索、快捷键、设置、回收站与托盘菜单等）；内容接入 i18n，支持简体中文、繁体中文、English、日本語、Deutsch、Français，切换语言时自动更新。
- **清除按查询条件**：「清除全部」改为按当前筛选条件清除（分类 / 时间 / 搜索），避免误清整库。
- **快捷键可视化**：菜单、按钮、托盘菜单显示快捷键提示；新增 `src/lib/shortcuts.ts` 统一管理；搜索快捷键由 `⌘K` 调整为 `⌘/`（原组合易被其它程序占用）。
- **全新图标**：应用图标与系统托盘图标更换为新设计。

### 🐛 修复

- 托盘菜单链接图标与主界面历史列表中的 `TypeIcon link` 保持一致（互锁双环样式）。
- 托盘菜单 `help / about / settings / open` 图标统一为中性灰，修复亮色模式下「关于系统」图标不可见的问题。

### 📚 文档

- 新增《跨设备同步技术设计文档》（`docs/clipstack-sync-design.md`），含架构拓扑、同步数据流、配置模型、组共享模型四张 SVG 图示。
- README 新增公众号关注章节；修正「简单体中文」笔误为「简体中文」；补充语言说明。

### 🔧 构建 / 杂项

- 升级 macOS 打包脚本，支持多架构（universal / x64 / aarch64）并修正产物路径。

> 说明：跨设备同步（「同步组」端到端加密共享）为下个大版本规划，本版本尚未上线，相关描述已从帮助与用户指南中移除。
